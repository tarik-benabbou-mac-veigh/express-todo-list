<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo list</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>My todo list as a junior web developer </h1>
    </header>
    <main>
        <h2>My tasks</h2>
        <form method="post" action="/task">
            <input type="text" name="task" placeholder="New task" autofocus>
            <button type="submit">Add</button>
        </form>
        <ul>
            <% tasks.forEach((task, idlist)=>{ %>
                <% if(task.done){ %>
                    <li><s><%= task.title %></s><a href="/task/<%= idlist%>/delete">Delete</a></li>
                    <% } else{ %>
                        <li><%= task.title %><a href="/task/<%= idlist %>/done">Finished</a></li>
                <% } %>
            <% }) %>
        </ul>
    </main>
</body>     
</html>