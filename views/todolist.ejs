<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo list</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>My todo list</h1>
    </header>
    <main>
        <h2>My tasks to become a junior back-end developer</h2>
        <ul>
            <% tasks.forEach((task)=>{ %>
                <% if(task.done){ %>
                    <li>
                        <%= task.title %><a href="#">Finished</a>
                    </li>
                    <% } else{ %>
                    <li>
                        <%= task.title %><a href="#">Delete</a>
                    </li>
                <% } %>
            <% }) %>
        </ul>
    </main>
</body>     
</html>