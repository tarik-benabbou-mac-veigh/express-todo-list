<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo list</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>My todo list</h1>
    </header>
    <main>
        <h2>My tasks to become a junior back-end developer</h2>
        <form method="post" action="/task">
            <input type="text" name="task" placeholder="New task" autofocus>
            <button type="submit">Add</button>
        </form>
        <ul>
            <% tasks.forEach((task, idx)=>{ %>
                <% if(task.done){ %>
                    <li><s><%= task.title %></s><a href="#">Delete</a></li>
                    <% } else{ %>
                        <li><%= task.title %><a href="/task/<%= idx %>/done">Finished</a></li>
                <% } %>
            <% }) %>
        </ul>
    </main>
</body>     
</html>